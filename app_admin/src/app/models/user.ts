// Holds user information a reduced version compared to the express version of the user data structure

export class User {
    email: string;
     // name: string;

    constructor()
    {
        this.email = '';
        // this.name = '';
    }
}
